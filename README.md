# Simon-Game
The Simon says game built using html css and js.
We keep track of the pattern using a simple array and match every thing user clicks with that of the patter.
For new pattern We use a random number function to generate a random number.
